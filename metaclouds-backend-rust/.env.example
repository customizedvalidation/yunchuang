# Server
SERVER_HOST=0.0.0.0
SERVER_PORT=8001

# Database (SQLite)
DATABASE_URL=sqlite:metaclouds.db?mode=rwc

# Auth
# Must be at least 32 characters. Generate with: openssl rand -base64 48
JWT_SECRET=dev-secret-change-me-please-32-chars-minimum-xxxxxxxx
JWT_EXPIRES_SECONDS=86400

# Logging
LOG_LEVEL=info
